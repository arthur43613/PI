Primerio rode os arquivos script.py e analizeLimpesa.R, eles vão entrar em loop, onde o script.py vai gerar dados
brutos e o .R vai limpalos, gerando um arquivos chamado dadosCorretosPI.csv.

Baixe as bibliotecas python:

pip install numpy
pip install pandas
pip install seaborn
pip install scikit-learn
pip install nltk
pip install plotly
pip install networkx
pip install ipywidgets
pip install ipykernel
pip install --upgrade nbformat
pip install pyspark